const path = "../assets/categoryIcons/";
const extension = ".png";

const logo = require("../assets/images/logo" + extension);
const icon = require("../assets/images/icon" + extension);
const banner = require("../assets/images/banner" + extension);
const send = require("../assets/images/send" + extension);

const beauty = require(path + "beauty" + extension);
const bills = require(path + "bills" + extension);
const car = require(path + "car" + extension);
const charity = require(path + "charity" + extension);
const cocktail = require(path + "cocktail" + extension);
const computer = require(path + "computer" + extension);
const customFields = require(path + "customFields" + extension);
const categories = require(path + "categories" + extension);
const education = require(path + "education" + extension);
const entertainment = require(path + "entertainment" + extension);
const finance = require(path + "finance" + extension);
const food = require(path + "food" + extension);
const fuel = require(path + "fuel" + extension);
const gift = require(path + "gift" + extension);
const groceries = require(path + "groceries" + extension);
const healtcare = require(path + "healtcare" + extension);
const holidays = require(path + "holidays" + extension);
const home = require(path + "home" + extension);
const income = require(path + "income" + extension);
const investment = require(path + "investment" + extension);
const kids = require(path + "kids" + extension);
const loans = require(path + "loans" + extension);
const other = require(path + "other" + extension);
const restaurant = require(path + "restorant" + extension);
const shoppingBag = require(path + "shoppingBag" + extension);
const shopping = require(path + "shopping" + extension);
const sport = require(path + "sport" + extension);
const taxes = require(path + "taxes" + extension);
const transport = require(path + "transport" + extension);
const travel = require(path + "travel" + extension);
const tshirt = require(path + "tshirt" + extension);
const work = require(path + "work" + extension);
const wallet = require(path + "wallet" + extension);

export default {
    logo,
    icon,
    beauty,
    bills,
    car,
    charity,
    cocktail,
    computer,
    education,
    entertainment,
    finance,
    food,
    fuel,
    gift,
    groceries,
    healtcare,
    holidays,
    home,
    income,
    investment,
    kids,
    loans,
    other,
    restaurant,
    shoppingBag,
    shopping,
    sport,
    taxes,
    transport,
    travel,
    tshirt,
    work,
    banner,
    send,
    wallet,
    categories,
    customFields,
};
